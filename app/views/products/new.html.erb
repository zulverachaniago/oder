<% content_for :title, "Tambah Produk Baru" %>
<div class="page-header">
  <div class="container">
    <%= render "layouts/partial/breadcrumb" %>
    <div class="d-flex align-items-center gap-3">
      <%= link_to products_path, class: "btn btn-outline-secondary" do %>
        <i class="bi bi-arrow-left me-2"></i>Kembali
      <% end %>
      <div>
        <h1 class="fw-bold mb-2" style="color: var(--primary-red);">
          <i class="bi bi-plus-circle me-2"></i>Tambah Produk Baru
        </h1>
        <p class="text-muted mb-0">Buat produk baru untuk ditambahkan ke menu</p>
      </div>
    </div>
  </div>
</div>

<div class="container mb-5">
  <%= render "form", product: @product %>
</div>
